# دليل التشغيل على Termux (الهاتف المحمول)
# Termux Mobile Installation Guide

## 📱 إعداد النظام على الهاتف

### 1️⃣ تثبيت Termux
```
📲 من Google Play أو F-Droid:
- Termux (مجاني)
- Termux:API (لإمكانيات إضافية)
- Termux:Boot (تشغيل تلقائي)
```

### 2️⃣ الأوامر في Termux

#### تثبيت Python والحزم:
```bash
# تحديث النظام
pkg update && pkg upgrade -y

# تثبيت Python
pkg install python -y
pkg install python-dev -y
pkg install openssl -y

# تثبيت الحزم الأساسية
pip install --upgrade pip
pip install requests
pip install flask
```

#### إنشاء مجلد النظام:
```bash
# إنشاء مجلد
mkdir -p ~/parental_control
cd ~/parental_control

# نسخ الملفات (استخدم الأوامر التالية)
```

### 3️⃣ تشغيل النظام

#### الخيار الأول - التشغيل التفاعلي:
```bash
python3 mobile_parental_control.py
```

#### الخيار الثاني - التشغيل في الخلفية:
```bash
# بدء المراقبة في الخلفية
nohup python3 mobile_parental_control.py > monitor.log 2>&1 &

# التحقق من التشغيل
ps aux | grep mobile_parental_control
```

#### الخيار الثالث - التشغيل التلقائي:
```bash
# مع Termux:Boot، سيتم التشغيل تلقائياً عند إقلاع الهاتف
```

### 4️⃣ الميزات المتاحة

#### ✅ مراقبة التطبيقات:
- تتبع التطبيقات المستخدمة
- تحديد مدة الاستخدام
- حجب التطبيقات غير المناسبة

#### ✅ مراقبة البحث:
- تسجيل عمليات البحث
- فحص سلامة المحتوى
- حجب الكلمات المفتاحية

#### ✅ حماية السرية:
- يعمل بصمت في الخلفية
- لا يظهر إشعارات للطفل
- حفظ البيانات محلياً فقط

#### ✅ التقارير:
- تقرير شامل للأنشطة
- إحصائيات الاستخدام
- حفظ في قاعدة البيانات

### 5️⃣ الاستخدام العملي

#### إضافة الأطفال:
عدّل ملف `mobile_config.json`:
```json
{
  "children": [
    {
      "name": "اسم الطفل",
      "age": 12,
      "device_id": "phone_001"
    }
  ]
}
```

#### تطبيق الحجب:
سيتم حجب التطبيقات والمواقع تلقائياً عند رصد محتوى غير مناسب.

### 6️⃣ نصائح الأمان

#### ✅ للأسرة:
- تأكد من موافقة الأطفال (إذا أمكن)
- ناقش قضايا الأمان الرقمي
- استخدم النظام كدعم، ليس كبديل للتواصل

#### ✅ للخصوصية:
- احفظ البيانات محلياً فقط
- لا تشارك معلومات شخصية
- راجع البيانات بانتظام

### 7️⃣ استكشاف الأخطاء

#### مشاكل شائعة:
```bash
# إذا لم يعمل Python
pkg install python

# إذا لم تثبت الحزم
pip install --user requests flask

# إذا لم تعمل قاعدة البيانات
chmod 600 mobile_parental_control.db
```

#### تحقق من النظام:
```bash
# عرض العمليات النشطة
ps aux | grep python

# عرض آخر الأنشطة
tail -f monitor.log

# فحص قاعدة البيانات
sqlite3 mobile_parental_control.db "SELECT * FROM activities;"
```

### 8️⃣ التحديث والصيانة

#### تحديث النظام:
```bash
# تحديث الحزم
pkg update

# تحديث ملفات النظام
# قم بنسخ الملفات المحدثة من الكمبيوتر
```

#### النسخ الاحتياطي:
```bash
# نسخ البيانات
cp mobile_parental_control.db ~/backup/
cp mobile_config.json ~/backup/
```

## 🎯 الخلاصة

هذا النظام يوفّر:
- 🔒 **حماية صامتة** - يعمل دون علم الطفل
- 📊 **مراقبة شاملة** - التطبيقات والبحث والمحتوى
- 📱 **سهولة الاستخدام** - واجهة بسيطة على الهاتف
- 🛡️ **أمان البيانات** - حفظ محلي محمي

**تذكر**: استخدم النظام كجزء من التربية الإيجابية، وليس كبديل للتواصل مع أطفالك.