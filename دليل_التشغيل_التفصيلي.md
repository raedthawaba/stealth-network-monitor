# 📱 دليل التثبيت والتشغيل على Termux - خطوة بخطوة
# Termux Installation & Running Guide - Step by Step

## المرحلة 1️⃣: تحضير الهاتف

### أ. تحميل التطبيقات:
1. **Termux** - من Google Play Store أو F-Droid
2. **Termux:API** - للحصول على مميزات إضافية
3. **Termux:Boot** - للتشغيل التلقائي (اختياري)

### ب. فتح Termux:
- انقر على أيقونة Termux
- ستجد شاشة سوداء مع أبيض (terminal)
- هذا هو المكان الذي ستكتب فيه الأوامر

---

## المرحلة 2️⃣: تحديث وتثبيت Python

### الأوامر في Termux (اكتب كل سطر واضغط Enter):

```bash
# تحديث النظام
pkg update && pkg upgrade -y

# تثبيت Python
pkg install python -y

# تثبيت الأدوات المساعدة
pkg install python-dev -y
pkg install openssl -y
pkg install curl -y

# التحقق من التثبيت
python --version
pip --version
```

**النتائج المتوقعة:**
```
Python 3.11.x
pip 23.x.x
```

---

## المرحلة 3️⃣: تثبيت المكتبات المطلوبة

```bash
# تحديث pip
pip install --upgrade pip

# تثبيت المكتبات الأساسية
pip install requests
pip install flask
pip install simplejson

# تثبيت أي حزم إضافية
pip install psutil 2>/dev/null || echo "psutil not available, will use alternatives"
```

**إذا واجهت أخطاء:**
```bash
# جرب التثبيت مع --user
pip install --user requests flask

# أو جرب الطريقة البديلة
pip install --no-cache-dir requests flask
```

---

## المرحلة 4️⃣: إنشاء مجلد النظام

```bash
# إنشاء مجلد العمل
mkdir -p ~/parental_control
cd ~/parental_control

# عرض المجلد الحالي
pwd
ls -la
```

**يجب أن ترى:**
```
/data/data/com.termux/files/home/parental_control
```

---

## المرحلة 5️⃣: إنشاء ملفات النظام

### أ. إنشاء ملف الإعدادات:

**في Termux، اكتب:**
```bash
cat > mobile_config.json << 'EOF'
{
  "mobile_settings": {
    "device_type": "android",
    "run_in_background": true,
    "stealth_mode": true,
    "notification_to_child": false
  },
  "monitoring": {
    "app_monitoring": true,
    "web_monitoring": true,
    "time_restrictions": true,
    "location_tracking": false
  },
  "children": [
    {
      "id": 1,
      "name": "احمد",
      "age": 12,
      "device_id": "android_device_001"
    },
    {
      "id": 2, 
      "name": "فاطمة",
      "age": 9,
      "device_id": "android_device_002"
    }
  ],
  "monitoring_rules": {
    "blocked_apps": [
      "instagram",
      "tiktok", 
      "snapchat",
      "telegram"
    ],
    "blocked_keywords": [
      "منتهي",
      "إباحي",
      "مؤثر",
      "عنف"
    ],
    "time_limits": {
      "weekday": 3,
      "weekend": 4
    },
    "safe_search": true
  }
}
EOF
```

### ب. إنشاء الملف الرئيسي:

**في Termux، اكتب:**
```bash
cat > mobile_parental_control.py << 'EOF'
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
نظام الرقابة الأبوية للهاتف المحمول
Mobile Parental Control System
"""

import os
import json
import sqlite3
import time
import threading
from datetime import datetime
import random

class MobileParentalControl:
    def __init__(self):
        self.config = self.load_config()
        self.running = False
        self.setup_database()
    
    def load_config(self):
        """تحميل إعدادات الهاتف"""
        try:
            with open('mobile_config.json', 'r', encoding='utf-8') as f:
                return json.load(f)
        except:
            return {
                "mobile_settings": {
                    "stealth_mode": True,
                    "background_monitoring": True
                },
                "children": [
                    {
                        "name": "الطفل الأول",
                        "age": 12,
                        "device_id": "phone_001"
                    }
                ],
                "monitoring_rules": {
                    "blocked_apps": ["instagram", "tiktok"],
                    "time_limits": {"weekday": 3, "weekend": 4},
                    "safe_search": True
                }
            }
    
    def setup_database(self):
        """إنشاء قاعدة البيانات"""
        conn = sqlite3.connect('mobile_parental_control.db')
        cursor = conn.cursor()
        
        cursor.execute('''
            CREATE TABLE IF NOT EXISTS activities (
                id INTEGER PRIMARY KEY AUTOINCREMENT,
                timestamp TEXT,
                child_name TEXT,
                activity_type TEXT,
                content TEXT,
                safety_score INTEGER,
                action_taken TEXT
            )
        ''')
        
        conn.commit()
        conn.close()
    
    def log_activity(self, child_name, activity_type, content, safety_score=100, action=""):
        """تسجيل نشاط"""
        conn = sqlite3.connect('mobile_parental_control.db')
        cursor = conn.cursor()
        
        cursor.execute('''
            INSERT INTO activities (timestamp, child_name, activity_type, content, safety_score, action_taken)
            VALUES (?, ?, ?, ?, ?, ?)
        ''', (datetime.now().isoformat(), child_name, activity_type, content, safety_score, action))
        
        conn.commit()
        conn.close()
    
    def check_content_safety(self, content):
        """فحص سلامة المحتوى"""
        blocked_keywords = [
            "منتهي", "إباحي", "مؤثر", "قاتل", "عنف", "مخدرات",
            "كحول", "تدخين", "انتحار", "تنمر", "كراهية"
        ]
        
        content_lower = content.lower()
        for keyword in blocked_keywords:
            if keyword in content_lower:
                return "unsafe", 10
        
        warning_keywords = ["لعبة", "فيديو", "موسيقى", "تريلر"]
        
        for keyword in warning_keywords:
            if keyword in content_lower:
                return "warning", 70
        
        return "safe", 100
    
    def simulate_monitoring(self):
        """محاكاة المراقبة"""
        print("🔍 بدء مراقبة الخلفية...")
        
        apps = [
            ("تطبيق Khan Academy", "safe", 15),
            ("تطبيق Instagram", "warning", 25),
            ("تطبيق YouTube Kids", "safe", 30),
            ("تطبيق TikTok", "unsafe", 20)
        ]
        
        searches = [
            "واجبات مدرسية", "كورس الرياضيات", "ألعاب الأكشن", 
            "مقاطع مضحكة", "البحث عن معلومات", "تعلم البرمجة",
            "محتوى غير مناسب", "أفلام الرعب", "موسيقى شعبية"
        ]
        
        for child in self.config['children']:
            child_name = child['name']
            print(f"\n👶 مراقبة الطفل: {child_name}")
            
            # محاكاة أنشطة
            for i in range(3):
                # تطبيق
                app_name, app_safety, duration = random.choice(apps)
                
                if app_safety == "unsafe":
                    action = "حجب التطبيق"
                    self.log_activity(child_name, "app_blocked", app_name, 10, action)
                    print(f"🚫 تم حجب: {app_name}")
                else:
                    action = "السماح"
                    self.log_activity(child_name, "app_used", app_name, 80, action)
                    print(f"✅ استخدام: {app_name}")
                
                time.sleep(1)
                
                # بحث
                search_term = random.choice(searches)
                safety_level, safety_score = self.check_content_safety(search_term)
                blocked = safety_level == "unsafe"
                
                if blocked:
                    action = "حجب البحث"
                    self.log_activity(child_name, "search_blocked", search_term, 10, action)
                    print(f"🚫 تم حجب البحث: {search_term}")
                else:
                    action = "السماح"
                    self.log_activity(child_name, "search_allowed", search_term, 80, action)
                    print(f"✅ البحث المسموح: {search_term}")
                
                time.sleep(1)
    
    def start_background_monitoring(self):
        """بدء المراقبة في الخلفية"""
        self.running = True
        
        def monitor_loop():
            while self.running:
                try:
                    self.simulate_monitoring()
                    time.sleep(30)  # انتظار 30 ثانية
                except Exception as e:
                    print(f"خطأ في المراقبة: {e}")
                    time.sleep(10)
        
        monitor_thread = threading.Thread(target=monitor_loop, daemon=True)
        monitor_thread.start()
        
        print("🚀 المراقبة بدأت في الخلفية...")
        print("📱 النظام يعمل بصمت - لا يظهر للطفل")
        
        return monitor_thread
    
    def stop_monitoring(self):
        """إيقاف المراقبة"""
        self.running = False
        print("⏹️ تم إيقاف المراقبة")
    
    def generate_report(self):
        """إنشاء تقرير"""
        print("\n" + "="*50)
        print("📊 تقرير الرقابة الأبوية")
        print("="*50)
        
        conn = sqlite3.connect('mobile_parental_control.db')
        cursor = conn.cursor()
        
        cursor.execute('''
            SELECT child_name, activity_type, content, safety_score, action_taken, timestamp
            FROM activities 
            ORDER BY timestamp DESC
            LIMIT 15
        ''')
        
        activities = cursor.fetchall()
        
        if activities:
            print("\n📝 آخر الأنشطة:")
            for activity in activities:
                print(f"🕐 {activity[5]} - {activity[0]}: {activity[2]} ({activity[3]}/100) - {activity[4]}")
        else:
            print("\n❌ لا توجد أنشطة مسجلة")
        
        conn.close()
        print("\n" + "="*50)
    
    def run_interactive(self):
        """تشغيل تفاعلي"""
        print("🛡️ نظام الرقابة الأبوية للهاتف المحمول")
        print("="*50)
        
        while True:
            print("\nاختر الخيار:")
            print("1. بدء المراقبة في الخلفية")
            print("2. إيقاف المراقبة") 
            print("3. عرض التقرير")
            print("4. محاكاة مراقبة سريعة")
            print("5. خروج")
            
            try:
                choice = input("\nاختيارك (1-5): ").strip()
                
                if choice == "1":
                    self.start_background_monitoring()
                    input("اضغط Enter للمتابعة...")
                
                elif choice == "2":
                    self.stop_monitoring()
                    input("اضغط Enter للمتابعة...")
                
                elif choice == "3":
                    self.generate_report()
                    input("اضغط Enter للمتابعة...")
                
                elif choice == "4":
                    print("🔄 تشغيل محاكاة سريعة...")
                    self.simulate_monitoring()
                    print("✅ انتهت المحاكاة")
                    input("اضغط Enter للمتابعة...")
                
                elif choice == "5":
                    print("👋 goodbye!")
                    self.stop_monitoring()
                    break
                
                else:
                    print("❌ اختيار غير صحيح")
            
            except KeyboardInterrupt:
                print("\n\n⏹️ إيقاف النظام...")
                self.stop_monitoring()
                break

if __name__ == "__main__":
    control = MobileParentalControl()
    control.run_interactive()
EOF
```

### ج. إعطاء صلاحيات التنفيذ:
```bash
chmod +x mobile_parental_control.py
```

### د. التحقق من الملفات:
```bash
ls -la
```

**يجب أن ترى:**
```
-rw-r--r-- 1 mobile_config.json
-rw-r--r-- 1 mobile_parental_control.py
```

---

## المرحلة 6️⃣: تشغيل النظام

### الخيار الأول - التشغيل التفاعلي:
```bash
python3 mobile_parental_control.py
```

**سأظهر لك قائمة، اختر:**
- 1 = بدء المراقبة
- 3 = عرض التقرير
- 4 = محاكاة سريعة

### الخيار الثاني - التشغيل المباشر:
```bash
# تشغيل محاكاة مباشرة
python3 -c "
from mobile_parental_control import MobileParentalControl
control = MobileParentalControl()
control.simulate_monitoring()
control.generate_report()
"
```

### الخيار الثالث - التشغيل في الخلفية:
```bash
# بدء المراقبة في الخلفية
nohup python3 mobile_parental_control.py > monitor.log 2>&1 &

# أو لتشغيل في المستقبل:
screen -S monitor python3 mobile_parental_control.py
```

---

## المرحلة 7️⃣: التحقق من العمل

### أ. فحص العمليات:
```bash
# عرض عمليات Python النشطة
ps aux | grep python

# عرض آخر الأنشطة
tail -f monitor.log
```

### ب. فحص قاعدة البيانات:
```bash
# فتح قاعدة البيانات
sqlite3 mobile_parental_control.db

# عرض الأنشطة
SELECT * FROM activities ORDER BY timestamp DESC LIMIT 5;

# خروج
.quit
```

### ج. عرض التقارير:
```bash
# تشغيل المراقبة وتوليد تقرير
python3 -c "
from mobile_parental_control import MobileParentalControl
control = MobileParentalControl()
control.simulate_monitoring()
control.generate_report()
"
```

---

## المرحلة 8️⃣: التشغيل التلقائي (اختياري)

### باستخدام Termux:Boot:
```bash
# تثبيت Termux:Boot
pkg install termux-boot

# إنشاء مجلد التشغيل التلقائي
mkdir -p ~/.termux/boot

# إنشاء ملف التشغيل
cat > ~/.termux/boot/start_monitor.sh << 'EOF'
#!/data/data/com.termux/files/usr/bin/bash
cd ~/parental_control
python3 mobile_parental_control.py &
EOF

# إعطاء صلاحيات
chmod +x ~/.termux/boot/start_monitor.sh
```

---

## نصائح مهمة:

### ✅ للخصوصية:
- احفظ البيانات محلياً فقط
- لا تشارك معلومات شخصية
- راجع البيانات بانتظام

### ✅ للاستخدام الآمن:
- علم الأطفال الأمان الرقمي
- استخدم النظام كدعم، ليس كبديل للتواصل
- حدث الإعدادات حسب نمو الأطفال

### ✅ استكشاف الأخطاء:
```bash
# إذا لم يعمل Python
pkg install python

# إذا لم تثبت الحزم
pip install --user requests flask

# إعادة تشغيل Termux
pkill -f python
```

### ✅ النسخ الاحتياطي:
```bash
# نسخ البيانات
cp mobile_parental_control.db ~/backup/
cp mobile_config.json ~/backup/

# إنشاء أرشيف
tar -czf parental_backup.tar.gz mobile_parental_control.db mobile_config.json
```

---

## 🎯 **مثال تطبيقي:**

**عند تشغيل النظام، ستشهد:**

```
🛡️ نظام الرقابة الأبوية للهاتف المحمول
==================================================

اختر الخيار:
1. بدء المراقبة في الخلفية
2. إيقاف المراقبة
3. عرض التقرير
4. محاكاة مراقبة سريعة
5. خروج

اختيارك (1-5): 4

🔄 تشغيل محاكاة سريعة...
🔍 بدء مراقبة الخلفية...

👶 مراقبة الطفل: احمد
✅ استخدام: تطبيق Khan Academy
🚫 تم حجب: تطبيق TikTok
✅ البحث المسموح: واجبات مدرسية
🚫 تم حجب البحث: محتوى غير مناسب

👶 مراقبة الطفل: فاطمة
✅ استخدام: تطبيق YouTube Kids
✅ البحث المسموح: كورس الرياضيات

✅ انتهت المحاكاة
اضغط Enter للمتابعة...
```

**هذا هو النظام جاهز للاستخدام! 🚀**